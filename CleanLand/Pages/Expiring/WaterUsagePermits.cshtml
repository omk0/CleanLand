@page
@model CleanLand.Pages.Expiring.WaterUsagePermitsModel
@{
    ViewData["Title"] = "Дозволи, що закінчуються";
}

<h2>@ViewData["Title"]</h2>

<table class="table">
    <thead>
    <tr>
        <th>Номер дозволу</th>
        <th>Дата початку</th>
        <th>Термін (роки)</th>
        <th>Дата закінчення</th>
        <th>Залишилось днів</th>
        <th>Пов'язані ставки</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model.ExpiringPermits)
    {
        <tr>
            <td>@item.Number</td>
            <td>@item.StartDate.ToShortDateString()</td>
            <td>@item.TermInYears</td>
            <td>@item.ExpirationDate.ToShortDateString()</td>
            <td>@item.DaysRemaining</td>
            <td>
                @foreach (var pond in item.Ponds)
                {
                    <span>@pond.Name, id: @pond.Id</span><br>
                }
            </td>
        </tr>
    }
    </tbody>
</table>
